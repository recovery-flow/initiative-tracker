openapi: 3.0.3
info:
  description: example
  title: test
  version: 0.0.0
servers:
- url: /
paths:
  /test:
    post:
      operationId: test
      responses:
        "201":
          description: Ok
      summary: Test
components:
  schemas:
    LinksDirect:
      properties:
        links:
          $ref: '#/components/schemas/LinksDirect_links'
        data:
          $ref: '#/components/schemas/LinksDirect_data'
      required:
      - links
      type: object
    LinksPagination:
      properties:
        previous:
          description: Link to the previous page
          type: string
        self:
          description: Link to the current page
          type: string
        next:
          description: Link to the next page
          type: string
      required:
      - next
      - previous
      - self
      type: object
    Owner:
      properties:
        first_name:
          description: first name of participant
          type: string
        second_name:
          description: second name of participant
          type: string
        third_name:
          description: third name of participant
          type: string
        display_name:
          description: name of participant
          type: string
        position:
          description: position in the company
          type: string
        desc:
          description: description of participant
          type: string
      required:
      - display_name
      - first_name
      - position
      - second_name
      type: object
    Initiative:
      properties:
        data:
          $ref: '#/components/schemas/Initiative_data'
        included:
          $ref: '#/components/schemas/Initiative_included'
      required:
      - data
      type: object
    InitiativeCreate:
      properties:
        data:
          $ref: '#/components/schemas/InitiativeCreate_data'
      required:
      - data
      type: object
    InitiativeUpdate:
      properties:
        data:
          $ref: '#/components/schemas/InitiativeUpdate_data'
      required:
      - data
      - included
      type: object
    Point:
      properties:
        data:
          $ref: '#/components/schemas/Point_data'
        included:
          items:
            $ref: '#/components/schemas/JarData'
          type: array
      required:
      - data
      type: object
    PointCreate:
      properties:
        data:
          $ref: '#/components/schemas/PointCreate_data'
      required:
      - data
      type: object
    PointUpdate:
      properties:
        data:
          $ref: '#/components/schemas/PointUpdate_data'
      required:
      - data
      type: object
    Jar:
      properties:
        data:
          $ref: '#/components/schemas/JarData'
      required:
      - data
      type: object
    JarUpdate:
      properties:
        data:
          $ref: '#/components/schemas/JarUpdate_data'
      required:
      - data
      type: object
    Participant:
      properties:
        data:
          $ref: '#/components/schemas/ParticipantData'
      required:
      - data
      type: object
    ParticipantData:
      properties:
        id:
          description: participant id
          type: string
        type:
          enum:
          - participant
          type: string
        attributes:
          $ref: '#/components/schemas/ParticipantData_attributes'
        relationships:
          $ref: '#/components/schemas/ParticipantData_relationships'
      required:
      - attributes
      - id
      - relationships
      - type
      type: object
    ParticipantCreate:
      properties:
        data:
          $ref: '#/components/schemas/ParticipantCreate_data'
      required:
      - data
      type: object
    ParticipantUpdate:
      properties:
        data:
          $ref: '#/components/schemas/ParticipantUpdate_data'
      required:
      - data
      type: object
    ParticipantsCollection:
      properties:
        data:
          items:
            $ref: '#/components/schemas/ParticipantData'
          type: array
        links:
          $ref: '#/components/schemas/LinksPagination'
      required:
      - data
      - links
      - meta
      type: object
    ParticipantCollection:
      properties:
        data:
          items:
            $ref: '#/components/schemas/ParticipantData'
          type: array
        links:
          $ref: '#/components/schemas/LinksPagination'
      required:
      - data
      - links
      - meta
      type: object
    JarData:
      properties:
        id:
          description: point id
          type: string
        type:
          enum:
          - jar
          type: string
        attributes:
          $ref: '#/components/schemas/JarAttributes'
        relationships:
          $ref: '#/components/schemas/JarData_relationships'
      required:
      - attributes
      - id
      - type
      type: object
    LinkSelf:
      properties:
        self:
          description: Link to participants
          type: string
      required:
      - self
      type: object
    InitiativeTagData:
      properties:
        id:
          description: tag id
          type: string
        name:
          description: tag name
          type: string
        weight:
          description: tag weight
          type: integer
      required:
      - id
      - name
      - weight
      type: object
    PointAttributes:
      properties:
        initiative_id:
          description: initiative id
          type: string
        level:
          description: level of point
          type: integer
        parent_id:
          description: parent id
          type: string
        title:
          description: title of point
          type: string
        desc:
          description: description of point
          type: string
        published_by:
          description: published by
          type: string
        local_cost:
          description: local cost
          format: double
          type: number
        local_collected:
          description: local collected
          format: double
          type: number
        status:
          description: status of point
          type: string
        created_at:
          description: point creation timestamp
          format: date-time
          type: string
        updated_at:
          description: point updated timestamp
          format: date-time
          type: string
      required:
      - created_at
      - desc
      - initiative_id
      - level
      - local_collected
      - local_cost
      - published_by
      - status
      - title
      type: object
    JarAttributes:
      properties:
        bank:
          description: bank of jar
          type: string
        url:
          description: url of jar
          type: string
      required:
      - bank
      - url
      type: object
    LinksDirect_links:
      properties:
        self:
          description: Link to participants
          type: string
        related:
          description: Link to participants
          type: string
      required:
      - related
      - self
      type: object
    LinksDirect_data:
      properties:
        id:
          description: resources id
          type: string
        type:
          description: resources type
          type: string
      required:
      - id
      - type
      type: object
    Initiative_data_attributes:
      properties:
        name:
          description: name of initiative
          type: string
        desc:
          description: description of initiative
          type: string
        goal:
          description: goal of initiative
          type: string
        verified:
          description: verified status
          type: boolean
        location:
          description: location of initiative
          type: string
        status:
          description: status of initiative
          type: string
        likes:
          description: likes of initiative
          format: int64
          type: integer
        reposts:
          description: reposts of initiative
          format: int64
          type: integer
        reports:
          description: reports of initiative
          format: int64
          type: integer
        created_at:
          description: Initiative created at
          example: 2021-01-01T00:00:00Z
          format: date-time
          type: string
        updated_at:
          description: Initiative updated at
          example: 2021-01-01T00:00:00Z
          format: date-time
          type: string
        closed_at:
          description: Initiative closed at
          example: 2021-01-01T00:00:00Z
          format: date-time
          type: string
      required:
      - created_at
      - desc
      - goal
      - likes
      - name
      - reports
      - reposts
      - status
      - verified
      type: object
    Initiative_data_relationships:
      properties:
        chat:
          $ref: '#/components/schemas/LinksDirect'
        likes:
          $ref: '#/components/schemas/LinksDirect'
        reposts:
          $ref: '#/components/schemas/LinksDirect'
        reports:
          $ref: '#/components/schemas/LinksDirect'
        participants:
          $ref: '#/components/schemas/LinksDirect'
        points:
          $ref: '#/components/schemas/LinksDirect'
      required:
      - chat
      - likes
      - participants
      - points
      - reports
      - reposts
      type: object
    Initiative_data:
      properties:
        id:
          description: initiative id
          type: string
        type:
          enum:
          - initiative
          type: string
        attributes:
          $ref: '#/components/schemas/Initiative_data_attributes'
        links:
          $ref: '#/components/schemas/LinkSelf'
        relationships:
          $ref: '#/components/schemas/Initiative_data_relationships'
      required:
      - attributes
      - id
      - links
      - relationships
      - type
      type: object
    Initiative_included:
      properties:
        tags:
          items:
            $ref: '#/components/schemas/InitiativeTagData'
          type: array
      required:
      - tags
      type: object
    InitiativeCreate_data_attributes:
      properties:
        name:
          description: name of initiative
          type: string
        desc:
          description: description of initiative
          type: string
        goal:
          description: goal of initiative
          type: string
        location:
          description: location of initiative
          type: string
        owner:
          $ref: '#/components/schemas/Owner'
      required:
      - desc
      - goal
      - name
      - owner
      type: object
    InitiativeCreate_data:
      properties:
        type:
          enum:
          - initiative_create
          type: string
        attributes:
          $ref: '#/components/schemas/InitiativeCreate_data_attributes'
      required:
      - attributes
      - type
      type: object
    InitiativeUpdate_data_attributes:
      properties:
        name:
          description: name of initiative
          type: string
        desc:
          description: description of initiative
          type: string
        goal:
          description: goal of initiative
          type: string
        location:
          description: location of initiative
          type: string
        status:
          description: status of initiative
          type: string
      type: object
    InitiativeUpdate_data:
      properties:
        type:
          enum:
          - initiative_update
          type: string
        attributes:
          $ref: '#/components/schemas/InitiativeUpdate_data_attributes'
      required:
      - attributes
      - type
      type: object
    Point_data_relationships:
      properties:
        parent:
          $ref: '#/components/schemas/LinksDirect'
        published_by:
          $ref: '#/components/schemas/LinksDirect'
        initiative:
          $ref: '#/components/schemas/LinksDirect'
        plan:
          $ref: '#/components/schemas/LinksDirect'
      required:
      - initiative
      - plan
      - published_by
      type: object
    Point_data:
      properties:
        id:
          description: point id
          type: string
        type:
          enum:
          - point
          type: string
        attributes:
          $ref: '#/components/schemas/PointAttributes'
        relationships:
          $ref: '#/components/schemas/Point_data_relationships'
      required:
      - attributes
      - id
      - relationships
      - type
      type: object
    PointCreate_data_attributes:
      properties:
        parent_id:
          description: parent id
          type: string
        level:
          description: level of point
          type: integer
        title:
          description: title of point
          type: string
        desc:
          description: description of point
          type: string
        local_cost:
          description: local cost
          format: double
          type: number
        jar:
          $ref: '#/components/schemas/JarAttributes'
      required:
      - desc
      - level
      - title
      type: object
    PointCreate_data:
      properties:
        type:
          enum:
          - point_create
          type: string
        attributes:
          $ref: '#/components/schemas/PointCreate_data_attributes'
      required:
      - attributes
      - type
      type: object
    PointUpdate_data_attributes:
      properties:
        level:
          description: level of point
          type: integer
        title:
          description: title of point
          type: string
        desc:
          description: description of point
          type: string
        local_cost:
          description: local cost
          type: integer
        status:
          description: status of point
          type: string
      type: object
    PointUpdate_data:
      properties:
        type:
          enum:
          - point_update
          type: string
        attributes:
          $ref: '#/components/schemas/PointUpdate_data_attributes'
      required:
      - attributes
      - type
      type: object
    JarUpdate_data:
      properties:
        type:
          enum:
          - jar_update
          type: string
        attributes:
          $ref: '#/components/schemas/JarAttributes'
      required:
      - attributes
      - type
      type: object
    ParticipantData_attributes:
      properties:
        user_id:
          description: user id
          type: string
        first_name:
          description: first name of participant
          type: string
        second_name:
          description: second name of participant
          type: string
        third_name:
          description: third name of participant
          type: string
        display_name:
          description: name of participant
          type: string
        position:
          description: position in the company
          type: string
        verified:
          description: verified status
          type: boolean
        desc:
          description: description of participant
          type: string
        role:
          description: User role
          type: string
        updated_at:
          description: User updated at
          example: 2021-01-01T00:00:00Z
          format: date-time
          type: string
        created_at:
          description: User created at
          example: 2021-01-01T00:00:00Z
          format: date-time
          type: string
      required:
      - created_at
      - display_name
      - first_name
      - position
      - role
      - second_name
      - user_id
      - verified
      type: object
    ParticipantData_relationships:
      properties:
        user:
          $ref: '#/components/schemas/LinksDirect'
      required:
      - user
      type: object
    ParticipantCreate_data_attributes:
      properties:
        user_id:
          description: user id
          type: string
        first_name:
          description: first name of participant
          type: string
        second_name:
          description: second name of participant
          type: string
        third_name:
          description: third name of participant
          type: string
        display_name:
          description: name of participant
          type: string
        position:
          description: position in the company
          type: string
        desc:
          description: description of participant
          type: string
        role:
          description: participant role
          type: string
      required:
      - display_name
      - first_name
      - position
      - role
      - second_name
      - user_id
      type: object
    ParticipantCreate_data:
      properties:
        type:
          enum:
          - participant_create
          type: string
        attributes:
          $ref: '#/components/schemas/ParticipantCreate_data_attributes'
      required:
      - attributes
      - type
      type: object
    ParticipantUpdate_data_attributes:
      properties:
        first_name:
          description: first name of participant
          type: string
        second_name:
          description: second name of participant
          type: string
        third_name:
          description: third name of participant
          type: string
        display_name:
          description: name of participant
          type: string
        position:
          description: position in the company
          type: string
        desc:
          description: Description
          type: string
        role:
          description: User role
          type: string
      type: object
    ParticipantUpdate_data:
      properties:
        type:
          enum:
          - participant_update
          type: string
        attributes:
          $ref: '#/components/schemas/ParticipantUpdate_data_attributes'
      required:
      - attributes
      - type
      type: object
    JarData_relationships:
      properties:
        initiative:
          $ref: '#/components/schemas/LinksDirect'
        point:
          $ref: '#/components/schemas/LinksDirect'
      required:
      - initiative
      - point
      type: object
