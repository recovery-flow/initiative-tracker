type: object
required:
  - data
properties:
  data:
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        description: "initiative_id"
      type:
        type: string
        enum: [ initiative_add_tag ]
      attributes:
        $ref: './InitiativeTagData.yaml'